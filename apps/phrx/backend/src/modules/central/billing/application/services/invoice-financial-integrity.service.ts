/** @deprecated Prefer `@skalway/billing` — shim de compatibilidade PhRx. */
export {
  assertInvoiceAmounts,
  computePayableAmount,
  computeRemainingAmount,
  deriveInvoiceStatus,
} from "@skalway/billing";
